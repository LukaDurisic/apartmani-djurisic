[build]
  command = "npm run build"
  publish = ".next"

# Official Next.js runtime for Netlify — handles SSR, image optimization,
# routing and the App Router build output automatically.
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"
